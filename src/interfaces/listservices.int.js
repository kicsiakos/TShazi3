"use strict";
/* import { TodoItem } from "../classes/todoItem.class";

export interface ListServices<K, T> {

    addItem(item: TodoItem<T>): void;
    deleteItem(item: TodoItem<T>): void;
    listItems(): Map<T> | undefined;

} */ 
